---
openapi: get /movie/changes
---

<Note>
        You can query this method up to **14 days** at a time.  
        Use the `start_date` and `end_date` query parameters.  
        **100 items** are returned per page.
</Note>